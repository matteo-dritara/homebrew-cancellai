# Gate Sensitivity

<!-- Generated by scripts/gate_sensitivity.py. Do not edit by hand. -->

Each row is a planted violation of a named claim, applied to a throwaway copy of this
repository, with the gate set run against it. **An invariant with no killing gate is
asserted, not verified.** This is Mills' error seeding, generalised from the two comparators
that already used it (`rust_python_parity.py self-test`, `diff_harness.py check`) to the
gates that guard the product.

| Mutant | Claim | Killed by | Gates run |
| --- | --- | --- | --- |
| `protected-name-removed` (SI-001) | Protected/unknown state is non-destructive | `pytest, characterization` | pytest, characterization |
| `protected-name-case-sensitive` (SI-006) | Protected-name barriers are defense in depth, and do not depend on spelling | `pytest` | pytest |
| `unreadable-reads-as-empty` (SI-008) | Partial scan is non-destructive | `pytest` | pytest |
| `second-deletion-path` (SI-019) | One mutation boundary: only the safety executor's platform seam deletes anything | `mutation-boundary-rust` | mutation-boundary-rust |
| `dependency-cycle` (SI-019) | The kernel does not depend on the crates it governs | `rust-workspace` | rust-workspace |
| `invariant-deleted` (SI-001) | The invariant set itself is stable and referenced | `docs` | docs |
| `story-risk-lowered` (process) | A story may not declare a risk level below the floor its paths set | `risk-classification` | risk-classification |
| `evidence-row-removed` (process) | Every acceptance criterion carries an evidence row | `evidence` | evidence |
| `unmanaged-component` (process) | A component present and absent from the manifest fails the gate | `agent-toolchain` | agent-toolchain |
| `skill-points-nowhere` (process) | A skill's pointers resolve | `agent-skills` | agent-skills |
| `story-status-forged` (process) | A story cannot sit past ready_for_review without committed evidence | `project-os` | project-os |

## Control: every gate on an unmutated copy

Without this the table above means nothing. A gate that fails on a clean export - because
the copy dropped something it needs - appears to kill every mutant it is pointed at.

- Gates exercised: **10**. Passing on a clean tree: **10**.
- Every gate passes on a clean tree, so each kill above is attributable to its mutant.
- Gates exercised here that no mutant has ever made fail: **0**.
- Gates this harness can run that `RELEASE_GATES.md` does not classify: **0**.

## Coverage

- Safety Invariants declared: **31**.
- Invariants with at least one mutant a gate kills: **4** (SI-001, SI-006, SI-008, SI-019).
- Mutants no gate caught: **0**.

The first number is the one to read honestly. Most invariants have **no mutant here at all**,
which is not the same as having no killing gate - it means nobody has yet written the
violation that would test them. Coverage grows by adding mutants, and a mutant that no gate
kills is a finding rather than a failure of this harness.

## What this cannot tell you

- A mutant is a violation somebody thought of. Seeded defects are systematically easier than
  real ones, because people plant what they already check for.
- Only fast, hermetic gates run. `cargo test`, `cargo clippy` and the differential parity
  gate are not exercised here, so a Rust invariant that only a compiled test can decide is
  outside this harness by construction.
- A gate that kills a mutant has been shown able to fail. It has not been shown to catch
  the class the mutant stands for.
- A kill can belong to the runtime rather than to this repository. The SI-008 mutant used to
  die on Python 3.13 and survive on 3.14, because the two versions differ on whether
  `Path.is_file()` re-raises `PermissionError` - so the report depended on the interpreter
  that produced it. It is now anchored on the program's own completeness channel. Anchors
  are unique by construction (`apply_mutant` refuses an ambiguous one), because a mutant
  that lands somewhere nobody chose certifies an invariant it never touched.
