{
  "schema_version": 1,
  "id": "E32",
  "phase": "P1",
  "title": "Safety Verdict Gate Reads Round History, Not Round Existence",
  "objective": "E12-S04's round-5 independent review (project/evidence/E12-S04-VERIFIER-REVIEW-ROUND5.md) found that `scripts/project_os.py`'s `safety_verdict_passes` cannot close any CR4 story whose Safety Verdict records more than one review round, because it treats any `FAIL`/`REJECT` line anywhere in the file as disqualifying, rather than reading the append-only round history the protocol requires (docs/development/AGENT_PROTOCOL.md's own multi-round precedent) for what it is: a passing final round after a corrected earlier one is not evidence the story is unsafe, it is the record of how it became safe. A verifier correctly found the implementation sound and could not record that finding through the gate. This epic exists so that defect lives in the backlog with its own story rather than inside a review record nobody re-reads (matching E29's own precedent for a review-discovered engineering-system defect).",
  "dependencies": [],
  "status": "in_progress",
  "stories": [
    {
      "id": "E32-S01",
      "title": "safety_verdict_passes reads the last verdict, not any verdict",
      "status": "in_progress",
      "outcome": "A Safety Verdict file's most recent standalone PASS/PASS_WITH_RESIDUALS/FAIL/REJECT line, by position in the file, determines whether the gate accepts it - not whether any such line exists anywhere in the file.",
      "change_risk": "CR2",
      "dependencies": [],
      "acceptance_criteria": [
        "If the most recent standalone verdict line in a Safety Verdict file is a PASS or PASS_WITH_RESIDUALS, then the gate shall accept the file even when an earlier round in the same file recorded FAIL or REJECT.",
        "If the most recent standalone verdict line in a Safety Verdict file is a FAIL or REJECT, then the gate shall refuse the file even when an earlier round in the same file recorded PASS or PASS_WITH_RESIDUALS - the existing test for this must keep passing unchanged.",
        "If a Safety Verdict file contains no standalone verdict line at all, then the gate shall refuse it, as today."
      ],
      "safety_obligations": [],
      "verification": [
        "Regression test: an append-only fixture with an early FAIL round followed by a later passing round is accepted.",
        "Regression test: the existing PASS-then-REJECT fixture stays refused.",
        "project/evidence/E12-S04/SAFETY_VERDICT.md itself is exercised as a real-world fixture at each of its own historical states."
      ],
      "documentation_impact": [
        "docs/development/AGENT_PROTOCOL.md"
      ]
    }
  ]
}
